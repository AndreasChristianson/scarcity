import React from 'react';

import ServerTime from './ServerTime';
import Login from './Login';

export default (props) => (
    <div className="box">
        <ServerTime />
        <Login />
    </div>
);
